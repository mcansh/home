name: Home Assistant Bridge
filter:
  # Automatically expose all of the following types of devices.
  include_domains:
    - camera
    - light
    - climate

entity_config:
  camera.driveway_cam:
    video_codec: copy
    audio_codec: copy
    support_audio: true
